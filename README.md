### in your terminal change your directory to /client
cd client

### install npm
npm install

### to run the website
npm run dev



### strucute and filesystem

# pages - contain the pages to be displayed
# components - contain the reusable componenets
# public - contain the images and media
# index.html - main DOM file over which the react renders
